import { Card, CardContent, Typography } from "@mui/material";

export default function PayrollAdminPanel() {
return (
    <Card>
    <CardContent>
        <Typography variant="h6">Payroll Management</Typography>
        <Typography>
        Admin payroll update UI can be expanded here
        </Typography>
    </CardContent>
    </Card>
);
}
